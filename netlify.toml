[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  autoLaunch = false

# SPA 路由：所有非 API/函数路径重定向到 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
